#ifndef DSY_ARDUINO_DSP
#define DSY_ARDUINO_DSP
#include "Arduino.h"
#include "utility/DaisySP/daisysp.h"
using namespace daisysp; // Likely we do not want to do this, but it makes it easier to read/use the DSP.
#endif
